export type PartData = {
  id: number;
  area: string;
  prefix: string;
  partCode: string;
  color: string;
  fullCode: string;
  createdAt: string;
};

export type PartFormData = {
  area: string;
  partCode: string;
  color: string;
};